# Use the single repo root .env for all variables (same database as marketing).
# Copy the root .env.example to .env at repo root: cp ../.env.example ../.env
# Required: DATABASE_URL, DIRECT_URL, NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY,
#           NEXTAUTH_URL, NEXTAUTH_SECRET
