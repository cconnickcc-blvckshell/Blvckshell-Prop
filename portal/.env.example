# Supabase Database Connection
# Use pooled DATABASE_URL for runtime (serverless-safe)
DATABASE_URL="postgresql://user:password@host:5432/database?pgbouncer=true&connection_limit=1"
# Use DIRECT_URL for Prisma migrations (bypasses pooler)
DIRECT_URL="postgresql://user:password@host:5432/database"

# Supabase Storage (for evidence and compliance documents)
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# NextAuth
# Local dev; on Vercel set to your production URL (e.g. https://your-app.vercel.app)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-nextauth-secret-key-change-in-production"

# App Configuration
NODE_ENV="development"
