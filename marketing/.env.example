# Use the single repo root .env for all variables (same database as portal).
# Copy the root .env.example to .env at repo root: cp ../.env.example ../.env
# Required: DATABASE_URL
# Optional: NEXT_PUBLIC_PORTAL_URL, NEXT_PUBLIC_SITE_URL
